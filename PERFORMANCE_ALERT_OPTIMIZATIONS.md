# Performance & Alert Management Optimizations

> **This file is archived.** The performance optimizations (frame skipping, feature caching,
> computational shortcuts) and alert management improvements (AlertCenter, AlertToast, severity
> sorting) described below are now part of the production baseline.
>
> For the current system state, see `docs/CURRENT_STATE.md`.
> For delivery status, see `docs/DELIVERY_CHECKLIST.md`.
