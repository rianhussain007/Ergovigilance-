# React Migration Plan

> **This file is archived.** The migration from Streamlit to React is complete.
> The product is now `ui_posture/` (React 19 + Vite 6 + Tailwind 4) +
> `backend_api/` (FastAPI) + `backend/` (AI core).
>
> See `README.md` and `docs/CURRENT_STATE.md` for the current architecture.
> See `ROADMAP.md` for remaining work.
