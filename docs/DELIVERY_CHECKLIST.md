# ErgoVigilance — Factory Delivery & QA Checklist

**Status date:** 2026-08-13 · **Branch:** master (clean tree) · **Target:** factory pilot deployment ("pivot product")

This is the single source of truth for what remains between "working dev system" and "deployable factory product." QA evidence is dated so nothing silently rots. Items are ordered by **earliest readiness** within each priority, with the owner marked:

- 🟦 **AGENT** — can be done now without human input (no camera, no factory, no approval needed)
- 🟩 **YOU** — requires you / a human on-site (labeling judgment, hardware, consent, decisions)
- 🟪 **JOINT** — agent prepares, you execute at the site

---

## Part 1 — QA evidence (verified 2026-08-13)

| Check | Result | Evidence |
|---|---|---|
| Backend test suite | ✅ PASS | `pytest -q` in `backend_api/` — all green, 1 skip (alerts 24/24, context engine 15/15) |
| Frontend typecheck | ✅ PASS | `npm run lint` (`tsc --noEmit`) clean |
| Frontend production build | ✅ PASS | `npm run build` — 23.5 s, only a >500 kB chunk warning (cosmetic, code-split in P1) |
| Health endpoint | ✅ PASS | `GET /health` → `{"status":"healthy","version":"0.1.0","model_available":true}` |
| API surface | ✅ PASS | 62 OpenAPI paths; `/api/trends` mock removed; real risk-trend / worker-trends / safety-report present |
| Endpoint latency (via frontend proxy) | ✅ PASS | sessions 13 ms · manager 21 ms · analytics 15 ms · alerts 29 ms · dashboard 8 ms |
| Alert lifecycle | ✅ PASS | resolve/acknowledge → clean 404 (not 500); 503 when live service uninitialized (hardened) |
| Metrics accuracy claim | ⚠️ CAVEATED — **not** human-ground-truth accuracy | Admin UI serves 76.86% (`0.7686`, hist_gradient_boosting) — a genuine held-out sklearn test-split accuracy, BUT **97.4% of training labels are auto-generated by `risk_from_features()` itself** (verified at row level: 4,794/4,794 multiposture rows have no usable hint; 972/1,126 office rows split HIGH/MEDIUM via the same function). It measures the model's self-consistency with its own thresholds, not real-world accuracy. **Zero human ground-truth labels exist** (no `ground_truth_*.json` anywhere; labeling pending). 76.86% fully replaced 97.97% in all served files; the old 97.97% survives only as historical records in git-tracked `results/fix_comparison.json` + `results/knee_angle_comparison.json` |
| Docker runtime | ✅ PASS | in-container login, PDF export (56.5 KB via container Playwright), persistence across down/up — all verified |
| Deployment packaging | ✅ PASS | Windows service scripts (`deploy/`), `docker-compose.yml`, `.env.production.example`, pilot checklist + consent one-pager + intake tracker |

**Known gaps found by this QA pass:**
1. ~~No automated frontend tests~~ — **CLOSED 2026-08-15**: `npm test` (vitest) with 5 smoke tests covering login → dashboard → sessions → alerts.
2. ~~>500 kB bundle chunk~~ — **CLOSED 2026-08-15**: routes code-split via `React.lazy`; warning gone, first-paint chunks are page-sized.
3. **`/api/alerts` currently returns 0 alerts** — expected (no live sessions running); the *day-one runbook* must confirm alert firing on a deliberate bad posture at the site. Not a defect. (P0-6)
4. **`docker-compose.verify.yml`** is a dev verification artifact (port overrides for this machine) — **not** the delivery file; ship `docker-compose.yml` with `.env`-driven ports. (P0-1)
5. **No real camera E2E** has run outside the dev machine's camera — the code path was verified in Docker, but framing/lighting/alert-trigger must be confirmed on-site. (P0-6)

---

## Part 2 — Remaining tasks by priority & earliest readiness

### P0 — Must be ready before/at the factory pilot

| # | Task | Owner | Earliest ready | Why |
|---|---|---|---|---|
| 1 | Make `docker-compose.yml` ports `.env`-driven (default 8000/5432/8080, overridable) so one file deploys on any clean machine; keep `docker-compose.verify.yml` dev-only | 🟦 AGENT | ✅ DONE 2026-08-15 | Ports now read `${POSTGRES_PORT:-5432}` / `${BACKEND_PORT:-8000}` / `${FRONTEND_PORT:-8080}`; overrides verified via `docker compose config` |
| 2 | Add frontend automated smoke tests (vitest): login → dashboard renders → sessions list → alert center loads | 🟦 AGENT | ✅ DONE 2026-08-15 | 5 vitest smoke tests (login→dashboard, live monitoring, sessions list, alert center, backend-down error) all pass; `npm test` wired |
| 3 | Code-split routes (lazy `React.lazy`) to kill the >500 kB chunk warning | 🟦 AGENT | ✅ DONE 2026-08-15 | `App.tsx` lazy-loads all 19 pages + Suspense fallback; build no longer warns >500 kB; `tsc --noEmit` clean |
| 4 | Ground-truth risk labeling of the 34 extracted frames — `label_frames.py --prelabel` makes it a ~1 h confirm/correct job | 🟩 YOU | as soon as you spend ~1 h | Produces the first honest accuracy number (the product's core credibility claim) |
| 5 | Run `scripts/evaluate_ground_truth.py` on those labels → `results/ground_truth_evaluation.json` → update docs with the real number | 🟦 AGENT | the hour after #4 lands | Turns labels into the honest accuracy figure |
| 6 | Day-one on-site smoke (from `docs/pilot/PILOT_DEPLOYMENT_CHECKLIST.md` §6): live feed renders, deliberate slouch fires an alert, stop → report + MP4 appear | 🟩 YOU | day 1 at the site | The single unverifiable-here gate; everything else is code-verified |
| 7 | Factory deployment: camera framing + consent (one-pager), LAN + firewall, Windows service install (or Docker), `.env` with real `AUTH_JWT_SECRET` + `CAMERA_SOURCES`, admin/supervisor logins, worker record | 🟩 YOU (+🟦 for a config walkthrough) | 1–2 days with the site | The pilot itself |

### P1 — Strengthen during pilot ramp

| # | Task | Owner | Earliest ready | Why |
|---|---|---|---|---|
| 8 | Task-pass labeling (5 classes) — no ergonomics expertise needed, can be done by anyone | 🟩 YOU | anytime | Task-recognition metrics don't need the ergonomics gate (guide §Phase 3 note) |
| 9 | Retrain risk classifier on real labels (Phase 4) vs 76.9% baseline | 🟪 JOINT (🟦 runs it) | after #4/#5 | The ML research track's first real number |
| 10 | Multi-camera / RTSP config test with ≥2 factory cameras | 🟩 YOU | at site | Multi-Camera page exists; needs real sources to verify |
| 11 | Multi-subject recordings (3 subjects × scenarios per guide) | 🟩 YOU | pilot weeks | Generalizability data — not required for the screening pilot itself |
| 12 | Ops drill: `Restart-Service ErgoVigilance`, log inspection (`deploy\logs\`), retention behavior | 🟩 YOU | before shift 1 | Rollback plan (§7 of pilot checklist) must be drilled, not just documented |
| 13 | Demo mode (`DEMO_MODE=true`) — seeds 30 synthetic sessions + alerts, fills dashboard, shows amber banner | 🟦 AGENT | ✅ DONE 2026-08-26 | `DEMO_MODE=true` env flag; synthetic sessions/alerts/dashboard via `demo_seeding.py`; `/api/demo-mode` endpoint; amber banner in Layout; tsc + vitest + build clean |

### P2 — Post-pilot / not needed for delivery

| # | Task | Owner | Notes |
|---|---|---|---|
| 13 | Phase K — Cloud & multi-site | — | Deliberately **not** needed: offline-first is the privacy/security story |
| 14 | Postgres-backed session cache | 🟦 | Only if session count outgrows the file cache (currently 99 files / ~400 ms cold scan — fine) |
| 15 | Load/perf under 24/7 shifts | 🟩 YOU | Watch `deploy\logs\` during week 1; report any drift |

---

## Part 3 — The three things that gate everything else

1. **Ground-truth labels** (P0-4) — the only code-independent gap between "76.9% (circular, caveated)" and "honest accuracy number." Everything else in the product is verified.
2. **The on-site day-one smoke** (P0-6) — hardware + framing + consent cannot be tested from this repo.
3. **The factory's own go/no-go** — intake tracker + consent + contact (all documented in `docs/pilot/`), execution is yours.

Everything else in Part 2 is either already done or agent-executable today.
