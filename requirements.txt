# NOTE: The historic root requirements.txt pinned the legacy Streamlit-era
# stack (streamlit, jupyter, torch, …) and is NOT what the product runs on.
# The product backend lives in backend_api/ — this file now redirects there so
# tooling that auto-reads a root requirements.txt installs the correct deps.
#
#   pip install -r requirements.txt        # == backend_api/requirements.txt
#   pip install -r backend_api/requirements-dev.txt   # + pytest, pip-audit
-r backend_api/requirements.txt
