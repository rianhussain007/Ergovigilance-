# Development / CI tooling on top of the production requirements.
-r requirements.txt

pytest>=8.0.0,<9.0.0
pip-audit>=2.7.0,<3.0.0
# starlette>=1.3.0's TestClient requires httpx2 (httpx is deprecated); without
# it, `pytest backend_api/tests` fails at collection with RuntimeError.
httpx2>=2.0.0,<3.0.0
