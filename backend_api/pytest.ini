[pytest]
testpaths = tests
addopts = -m "not hardware" -q
markers =
    hardware: requires a physical camera and/or the pose model (opt-in, run with -m hardware)

# Silence the expected dev-secret warning during tests (DEBUG defaults to true).
filterwarnings =
    ignore:Using the development-default AUTH_JWT_SECRET:UserWarning
